import getters from './getters';
import mutations from './mutation';
import actions from './action';

export default {
  state() {
    return {
      userId: 'c3',
    };
  },
  mutations,
  actions,
  getters,
};
